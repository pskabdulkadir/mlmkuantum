[build]
command = "pnpm install && pnpm --filter @workspace/akngroup run build:client"
publish = "artifacts/mlm-app/dist/spa"

[build.environment]
NODE_ENV = "production"
PNPM_FLAGS = "--no-frozen-lockfile"
